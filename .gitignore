# tmux plugins (each one is its own repo)
tmux/plugins/





############################
# OS Garbage
############################
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*
.Spotlight-V100

############################
# Editors / temp files
############################
*.swp
*.swo
*~
*.tmp
.netrwhist
Session.vim
tags

############################
# Backups / old copies (from your tree)
############################
/nvim.backup/
/nvim.backup.newest/
/old_nvim/

############################
# Neovim build/cache noise (keep your actual config)
############################
# (These only ignore common generated bits if you keep them inside the repo)
nvim/.cache/
nvim/pack/
nvim/site/pack/
nvim/plugin/packer_compiled.lua
nvim/lazy-lock.json
nvim/**/node_modules/

############################
# Secrets / tokens / machine-specific state
############################
/github-copilot/           # contains auth/state
/gh/                       # GitHub CLI: hosts.yml tokens
/Microsoft\VisualStudio Services/  # Azure DevOps creds & cache
/azure-datalake-store/     # service creds/state (if present)

############################
# App-specific state you likely don't want in git
############################
/AVer_eCam/                # vendor app data
/jgit/                     # tool state/cache
/graphite/                 # (if just tool cache; keep if you store config here)
/xbuild/                   # mono/xbuild cache; keep if you're intentionally tracking

############################
# Raycast (keep config, ignore caches)
############################
raycast/**/Cache*/
raycast/**/Local Storage*/
raycast/**/IndexedDB*/

############################
# iTerm2 (keep prefs if you export them; ignore autosaves)
############################
iterm2/**/AutoSave*
iterm2/**/RecentSessions*

############################
# Flutter (only if you keep flutter stuff here)
############################
flutter/.dart_tool/
flutter/.pub-cache/
flutter/.packages
flutter/.flutter-plugins
flutter/.flutter-plugins-dependencies
